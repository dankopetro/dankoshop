{
  "buildCommand": "npm run build --workspace=storefront",
  "installCommand": "npm install --registry=https://registry.npmmirror.com --legacy-peer-deps",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
